import {randomId} from "../../Functions";
import {items} from "../../mock-data";

const initialState = {
    orders: []
}

export default (state = initialState, action) => {
    switch (action.type) {
        default:
            return state
    }
}